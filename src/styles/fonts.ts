import { Marvel } from "next/font/google";
import { Anton } from "next/font/google";

export const marvel = Marvel({ subsets: ["latin"], weight: ["400", "700"] });
export const anton = Anton({ subsets: ["latin"], weight: ["400"] });