import React from "react";

const CharacterNameDetails = ({ charName }: { charName: string }) => {
  return <h2 className="text-3xl font-semibold">{charName}</h2>;
};

export default CharacterNameDetails;
